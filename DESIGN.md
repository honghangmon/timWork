# DESIGN.md (초안)

## 데이터 분석: metadata.json을 어떻게 해석했나요? 데이터 구조를 어떻게 이해했나요? 더 나은 데이터 표현 방법이 있을까요?

처음에는 전체 구조가 꽤 낯설게 느껴졌습니다. 건축 도면을 많이 접해본 적이 없었고, 용어도 익숙하지 않아서 `drawing`, `disciplines`, `regions`, `revisions`가 각각 어떤 역할을 하는지 구분하는 데 시간이 걸렸습니다.  
하지만 하나씩 뜯어보면서, 이 데이터는 **전체 배치도 → 개별 공간(동/시설) → 공종 → 필요하면 세부 구역(region) → 리비전(revision)** 으로 내려가는 구조라고 이해했습니다.

즉 큰 틀에서 작은 틀로 내려가면서 탐색하는 구조이고, 한 번 이해하고 나니 꽤 합리적이고 재사용성이 있는 데이터라고 느꼈습니다. 특히 공종별로 별도 도면이 있고, 어떤 공종은 다시 A/B 같은 세부 구역으로 나뉘며, 그 안에서 리비전이 관리된다는 점은 실제 도면 관리 방식이 데이터에 그대로 반영된 것 같았습니다.

다만 아쉬웠던 점도 있었습니다. 현재는 `drawings` 아래에 `"00"`, `"01"`, `"09"`, `"13"`이 모두 같은 레벨에 존재하는데, 실제 역할은 조금 다릅니다. `"00"`은 전체 기준 도면이고, `"01"`, `"09"`, `"13"`은 그 아래에 들어가는 하위 공간 도면이기 때문에, 구조적으로는 **전체 배치도와 개별 공간 도면이 한 단계 분리되어 있으면 더 이해하기 쉬웠을 것 같다**고 생각했습니다. 

또 하나는 기준 도면의 통일성입니다. 예를 들어 `101동`은 기준 도면이 건축으로 시작하는데, `주차장`은 기준 도면이 구조로 시작합니다. 실무적으로는 충분히 그럴 수 있다고 생각하지만, 과제를 접하고 이 부분이 다소 헷갈렸습니다. 어떤 것은 `drawing` 자체가 건축 기준이고, 어떤 것은 구조 기준이기 때문에, 이를 코드와 UI로 해석할 때 “무엇이 기준 도면이고 무엇이 공종 도면인지”를 계속 구분해야 했습니다.


---

## 접근 방식: 전체 과제를 어떤 순서와 방식으로 해결하셨나요?

전체적으로는 과제 내용을 먼저 여러 번 읽어보면서,  
이 과제의 핵심이 “도면을 예쁘게 보여주는 것”보다도 **사용자가 원하는 도면을 빨리 찾고, 지금 무엇을 보고 있는지 헷갈리지 않게 만드는 것**이라고 생각했습니다.  
그래서 저는 처음부터 **잘 찾을 수 있게 하기**와 **도면을 보기 편하게 하기** 두 가지를 가장 중요한 목표로 두었습니다.

진행 순서는 대략 아래와 같습니다.

1. 과제 요구사항과 사용자 시나리오를 먼저 정리했습니다.  
   특히 “최신 리비전 확인”, “공종 간 겹쳐보기”, “리비전 비교”가 핵심 시나리오라고 판단했습니다.
2. 그 다음 메타데이터 구조를 이해하는 데 시간을 많이 들였습니다.  
   이 과제는 UI를 바로 만드는 것보다, 데이터를 제대로 해석하지 못하면 뒤에서 계속 꼬일 수 있다고 생각했기 때문입니다.
3. 메타데이터를 해석한 뒤,  
   어떤 단위로 사용자가 도면을 탐색해야 자연스러운지 정리했습니다.  
   이 과정에서 `drawing`, `discipline`, `region`, `revision`의 역할을 나눠서 이해하려고 했습니다.
4. 이후 전체 UI 구조를 먼저 설계하고,  
   와이어프레임 수준에서 화면 구성을 정리했습니다.  
   이때 가운데는 최대한 도면을 크게 보여주고, 오른쪽 패널은 탐색과 조작에 집중하도록 방향을 잡았습니다.
5. 마지막으로 기능 단위로 컴포넌트를 나누어 구현했습니다.  
   메타데이터 파싱, 트리 구성, 기본 보기, 오버레이, 비교 기능을 순서대로 붙이면서 작업했습니다.

전체적으로는 “바로 구현”하기보다,  
**데이터를 해석하고 → 사용자 흐름을 정리하고 → 그에 맞는 UI를 만드는 순서**로 접근하려고 했습니다.

---

## UI 설계 결정: 어떤 레이아웃을 선택했고, 왜 그 방식이 최선이라고 판단했나요? 고려했던 대안들과 각각의 장단점은 무엇이었나요?

먼저 이 서비스를 사용할 사람을 현장 소장급 사용자라고 가정했습니다.  
젊어도 30대 중반, 많게는 50대 이상까지 사용할 수 있다고 생각했고,  
그래서 미적인 요소보다는 **직관적이고 편안하며, 오래 봐도 피로하지 않은 UI**가 더 중요하다고 판단했습니다.

그래서 전체 레이아웃은 크게 **Header + Viewer + Right Panel** 구조로 잡았습니다.

- **Header**: 현재 어떤 도면을 보고 있는지와 전체 맥락을 보여주는 영역
- **Viewer**: 도면을 최대한 크게 보여주는 핵심 영역
- **Right Panel**: 도면 선택, 리비전 확인, 오버레이/비교 기능을 수행하는 조작 영역

이 구조를 선택한 이유는, 이 과제에서 가장 중요한 것은 결국 **도면 자체를 크게, 방해 없이 보여주는 것**이라고 생각했기 때문입니다.  
중앙에 충분한 화면을 확보하고, 선택이나 비교 같은 조작은 한쪽 패널로 몰아주는 편이 가장 자연스럽다고 판단했습니다.

처음에는 왼쪽 패널도 두는 구조를 생각했습니다.  
왼쪽 패널은 `01`, `09`, `13`처럼 기준이 되는 도면을 선택하는 용도였습니다.  
하지만 실제로 작업을 진행해보니, 필요한 기능에 비해 화면을 너무 많이 차지한다고 느꼈습니다.  
결국 가장 중요한 건 도면을 크게 보는 것이기 때문에, 고정된 왼쪽 패널보다는 **필요할 때만 간단히 접근할 수 있는 토글 형태**가 더 낫다고 판단했습니다.

또 폴리곤 정보도 더 적극적으로 쓰는 방안을 고민했습니다.  
하지만 실제 도면 화면 위에 폴리곤이 계속 올라가 있으면 도면을 읽는 데 오히려 방해가 될 수 있다고 생각했습니다.  
그래서 폴리곤은 **가장 상위 진입 화면에서만 적극적으로 사용하고**,  
실제 도면을 자세히 보는 화면에서는 최대한 방해하지 않는 쪽으로 정리했습니다.

고려했던 대안은 두 가지 정도였습니다.

### 대안 1. 왼쪽 탐색 패널 + 중앙 도면 + 오른쪽 정보 패널
- 장점: 구조적으로 익숙하고 탐색 흐름이 명확함, 역할 분리가 쉬움
- 단점: 화면이 많이 좁아짐, 도면을 크게 보여주기 어려움

### 대안 2. 전체 화면 도면 중심 + 패널 최소화
- 장점: 도면을 가장 크게 볼 수 있음, 시각적으로 깔끔함
- 단점: 탐색/비교 기능을 숨긴 채로 넣기 어려움, 현재 위치 맥락을 잃기 쉬움

결국 저는 **도면을 크게 보여주되, 오른쪽 패널을 통해 탐색과 조작을 모두 수행하는 현재 구조가 가장 균형이 좋다**고 판단했습니다.

---

## 기술 선택: 상태 관리, 스타일링 등을 어떤 기준으로 선택했나요?

이번 과제에서는 아래와 같은 구조를 사용했습니다.

- 화면 전환 구조: `Splash -> Hub -> Explorer`
- 라우팅 라이브러리: 미사용 (`react-router-dom` 없음)
- 상태관리 라이브러리: 미사용 (Redux / Zustand / Jotai 없음)
- 상태관리 방식: React Hooks (`useState`, `useMemo`, `useCallback`, `useEffect`) 중심
- 폴더 구조:
  - `src/features/explorer`: 도면 탐색 기능(뷰어 / 우측 패널 / compare)
  - `src/core`: 메타데이터 파이프라인(로더 / 파서 / 인덱서 / 셀렉터 / 타입)
  - `src/sharedComponents`: 공용 UI 컴포넌트

상태 관리 도구를 아예 고려하지 않은 것은 아닙니다.  
다만 이번 과제에서는 데이터를 해석하고 구조를 정리하는 데 시간이 많이 들어갔고,  
전역 상태 라이브러리를 추가하기보다 **현재 필요한 상태를 React 기본 훅으로 관리하는 쪽이 더 빠르고 명확하다고 판단**했습니다.

스타일링도 마찬가지로, 화려한 UI보다는 **읽기 편하고 안정적인 인터페이스**를 우선으로 두었습니다.  
도면 뷰어와 오른쪽 패널이 핵심이기 때문에, 장식적인 효과보다는 버튼 상태, 선택 상태, 계층 구조가 잘 드러나도록 신경 썼습니다.

---

## 어려웠던 점 및 개선 방안: 과제를 수행하며 겪었던 어려움은 무엇이었고, 시간이 더 주어진다면 어떤 부분을 개선하고 싶으신가요?

가장 어려웠던 부분은 **메타데이터 구조를 해석하는 일 자체**였습니다.

특히 `01`, `09`, `13`처럼 기준이 되는 도면들이 각기 다른 방식으로 사용되고 있다는 점이 많이 헷갈렸습니다.  
예를 들어 `101동`은 `drawing`의 기준 도면이 건축으로 시작하고, `disciplines` 안에 다시 건축의 `REV1`이 존재합니다.  
반면 `주차장`은 루트에 있는 `13` 자체가 구조 기준 도면으로 되어 있고, 그 아래 구조 `REV1`도 같은 이미지처럼 보이기 때문에,  
처음에는 “이게 같은 도면을 두 번 표현한 건지, 아니면 다른 의미를 가진 건지”를 계속 구분해야 했습니다.

즉 어떤 곳은 **건축 → 건축 REV1** 구조이고,  
어떤 곳은 **구조 → 구조 REV1** 구조처럼 보이는데,  
이 둘이 완전히 같은 규칙으로 읽히지 않아서 일관성을 잡기가 쉽지 않았습니다.

오버레이 기능도 어려웠던 부분 중 하나였습니다.  
현재는 기준 도면과 직접 맞는 항목만 overlay 가능하도록 제한하고, 공통 opacity를 적용하는 쪽으로 정리했는데,  
시간이 더 있었다면 **겹쳐진 도면마다 개별 opacity 조절**이 가능하도록 만드는 방향도 고민해보고 싶습니다.

오버레이 위치를 더 정확하게 맞추고 싶었는데, 이미지 크기와 기준점이 조금씩 달라서 생각보다 쉽지 않았습니다.  
AI의 도움도 받아봤지만, 실제로는 transform 값을 더 세밀하게 검증하고 조합하는 로직이 필요하다고 느꼈습니다.  
이 부분은 시간이 부족해서 완벽히 구현하지 못했습니다.

또 폴리곤 데이터도 더 적극적으로 활용하고 싶었습니다.  
앞서 말했 듯, 실제 도면 화면 위에 폴리곤을 계속 노출하면 도면 자체를 읽는 데 방해가 될 수 있다고 판단해서,  
현재는 가장 첫 진입 화면에서만 제한적으로 활용했습니다. 분명 더 활용할 방안이 있을 것 같은데 생각하지 못하였습니다.

마지막으로, 줌 인터랙션도 더 개선하고 싶었습니다.  
마우스 휠 줌과 트랙패드 pinch zoom까지 지원하면 보는 경험이 훨씬 좋아질 것 같았지만, 이번에는 구현 범위에서 우선순위를 낮췄습니다.

시간이 더 주어진다면 아래 부분들을 추가로 개선하고 싶습니다.

- 도면 정렬 정확도 개선
- 개별 overlay opacity 조절
- 폴리곤 인터랙션 개선
- 마우스/트랙패드 줌 인터랙션 추가
- 상태 관리와 캐싱 구조 정리