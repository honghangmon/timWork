# 이미지 미출력 원인 분석 (Unicode 정규화 이슈)

## 1) 현상
- `dev`에서 `npm run dev` 실행 시 도면 이미지가 일부/전체 미출력됨.
- 같은 프로젝트인데 시점에 따라 보였다가 안 보이는 현상이 발생함.

## 2) 결론 (문제 레벨)
- 이 이슈는 단일 원인이 아니라, 아래 3개 레벨이 겹친 문제다.
1. 애플리케이션 레벨: 이미지 경로 정규화 시 `NFD`를 강제하던 로직
2. Git/OS 레벨: 파일명 유니코드 정규화(`NFC` vs `NFD`) 차이
3. 운영 절차 레벨: Git 추적/체크아웃 이후 파일명 정규화 상태가 바뀌면서 mismatch가 노출됨

## 3) 확인된 사실

### 3.1 Git 설정
- `core.precomposeunicode=true`
- 의미: macOS에서 Git이 파일명 정규화를 precomposed(`NFC`) 기준으로 다루도록 유도함.

### 3.2 현재 저장소 파일명 상태
- `public/drawings` 내 22개 파일 모두 `NFC`.
- 점검 결과: `nfc:22, nfd:0`.

### 3.3 앱 경로 생성 로직(문제 지점)
- 기존 `normalizeDrawingImageRef`가 파일명을 `normalize("NFD")`로 강제.
- 즉 실제 파일명(`NFC`)과 요청 URL(`NFD`)이 바이트 단위로 달라져 정적 파일 매칭 실패(404).

## 4) 왜 “Git 올리기 전에는 보이고, 이후엔 안 보였나”
- 핵심은 **push 자체**가 아니라, Git 추적/체크아웃 이후 파일명 정규화 상태 변화다.
- 합리적 시나리오:
1. 초기 로컬 파일 상태에서는 경로/파일명 정규화가 우연히 일치해서 표시됨.
2. Git에 추가/체크아웃/브랜치 전환/풀 과정에서 파일명이 `NFC` 기준으로 정리됨.
3. 앱은 계속 `NFD` URL을 생성해서 mismatch가 발생.

주의:
- 현재 시점에는 “Git 올리기 직전 파일명이 정확히 NFD였는지”를 100% 재현 증명할 수는 없다.
- 다만 현재 증거(저장소/파일은 NFC, 앱 URL은 NFD 강제)로 볼 때 원인 체인은 명확하다.

## 5) 다른 컴퓨터에서 clone 시 영향
- 이 문제는 재현 가능성이 높다.
- 이유:
1. 저장소 파일명은 `NFC`로 기록됨.
2. 클라이언트 코드가 `NFD` URL을 생성하면 OS/브라우저/서버 조합에 따라 파일 미매칭이 발생할 수 있음.
- 따라서 clone 대상 컴퓨터가 달라도, 코드 정규화 정책이 잘못되어 있으면 동일 문제가 재발할 수 있다.

## 6) 재발 방지 기준

### 필수 원칙
- 이미지 파일명과 URL 생성 로직을 **단일 정규화 기준(`NFC`)**으로 통일한다.
- 경로 생성 시 임의로 `NFD`를 강제하지 않는다.

### 권장 원칙
- 가능하면 한글 파일명 대신 ASCII 파일명 정책을 도입한다.
- CI/사전 점검 스크립트로 `metadata.json`의 이미지 참조가 실제 파일과 매칭되는지 자동 검증한다.

## 7) 빠른 점검 체크리스트
- `git config --get core.precomposeunicode`
- `public/drawings` 파일명 정규화 분포 확인(`NFC/NFD`)
- 앱에서 생성한 최종 이미지 URL과 실제 파일명을 같은 정규화 기준으로 비교
- dev 서버 Network 탭에서 이미지 요청 404 여부 확인

---

요약:
- 본 이슈는 “운영체제 문제만”도 아니고 “Git 문제만”도 아니다.
- **앱의 NFD 강제 로직 + Git/OS의 NFC 파일명 상태**가 충돌한 경로 정규화 버그다.
